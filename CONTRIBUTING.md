# 参与贡献

欢迎改进体验、补充适配和测试。请先阅读 README、docs/development.md 和 SECURITY.md。

## 开始前

- 页面结构样本优先使用最小化、去标识的 DOM 或本地模拟夹具，不提交真实画师图片和私人记录。
- 不提交 Cookie、验证码、登录状态、浏览器配置、下载目录、测试证书或个人授权文件。
- 不引入验证码绕过、代理轮换、账号轮换、隐蔽自动采集或远程代码加载。
- 改变权限、外部请求或数据处理方式时，先说明用途、边界和替代方案。

## 本地流程

1. 从独立分支修改。
2. 修改界面规则时优先补 `tests/unit/`；影响导航或下载时补本地浏览器夹具。
3. 运行 `npm run check`、`npm test`、`npm run format:check`。
4. 如有必要运行 `python tests/e2e/run.py`；不要用真实账号作为 CI 夹具。
5. 修改用户指南后运行 `python tools/render_docs.py` 和 `npm run format`。
6. 在 PR 中说明行为变化、测试范围、已知限制、权限和隐私影响。

保持一次 PR 聚焦一个主题。不应把模拟站点测试表述为所有真实页面兼容，也不要保证账号不受限。

## 问题反馈

使用仓库 Issue 模板。优先附上工作台生成的诊断信息，发布前再自行检查一次。完整日志、截图和任务备份可能包含私人链接或作品，不要直接上传。

安全漏洞请按 SECURITY.md 处理，不在公开 Issue 中放凭据或可直接滥用的细节。
