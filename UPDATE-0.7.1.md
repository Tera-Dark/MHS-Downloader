# 0.7.1：配对连接修复与更新步骤

## 修复了什么

### 1. “配对了，还是未连接”的根因

已在真实加载扩展的 Chromium 中复现：

- `POST /v1/pair` 带 `Origin: chrome-extension://…`，旧版服务接受请求，配对成功。
- 后续 `GET /v1/state`、`GET /v1/next` 不带 Origin，旧版服务一律拒绝，返回 403。
- 因而正确令牌也无法读取状态或推进采集。旧 UI 把状态读取错误放到下方队列区域，又没有配对成功提示，造成“点击后没反应”的体验。

0.7.1 的修复：

- 扩展显式发送 `X-Archive-Extension: <扩展 ID>`，兼容不带 Origin 的请求。
- **这个头不是密码**：仍需正确 Bearer 令牌；除配对外，还需已登记的扩展标识。
- 如果请求带 Origin，它必须是匹配的扩展来源；普通网站、`null` 来源和标识不一致的请求仍拒绝。
- 保留固定回环 Host 检查，不开放公网监听，也不使用 `Access-Control-Allow-Origin: *`。
- 只有“服务版本检查 → 配对 → GET 状态读取”全部成功后，界面才显示连接成功。
- 新增无需令牌的 `/v1/health`，只暴露服务名称、版本和协议号，不暴露任务、令牌或磁盘路径。
- 旧服务未退出、令牌错误、服务离线、接口不兼容都会显示可操作的提示。

### 2. 移除许可勾选门槛

官网面板、控制台表单、本机 API 三处均移除了强制勾选/拒绝条件。不通过偷偷传 `consent: true` 来假装用户确认，而是完全不再要求该字段。

保留简短的非阻塞使用提示。旧数据库无需迁移；为兼容旧表结构，新任务 `consent_at=0` 表示“未收集确认”，不是“已经获得许可”。历史任务原记录保留。

这项改动不取消网站登录、验证码、限流与权限边界，也不取消下载的图片校验。

### 3. 启动器适配你的 Python 安装方式

不再只检测 `py`。按以下顺序查找可运行的 Python 3.11+：

1. 可选的 `ARCHIVE_PYTHON` 自定义路径。
2. 已有且仍可运行的 `.venv`。
3. `%LOCALAPPDATA%\Programs\Python\Python312\python.exe` 等常用路径（312、313、314、311）。
4. Program Files 下的相应安装路径。
5. 可选的 `py` 启动器。
6. PATH 中的 Python，跳过 WindowsApps 的商店执行别名。

你提供的 `C:\Users\…\AppData\Local\Programs\Python\Python312\python.exe` 属于第 3 类，无需把个人用户名写死在发布包中。

启动时打印实际选中的 Python 路径与版本。保留正常的虚拟环境；发现失效 `.venv` 时给出明确说明，不删除数据。Windows BAT 使用 UTF-8 内容和 CRLF 行尾。

### 4. 操作反馈

- 配对按钮显示“正在连接…”，成功/失败提示紧贴配对区域。
- 顶部显示已连接或连接失败，不再把失败藏到页面底部。
- 添加任务显示“正在提交 → 已加入队列 / 已存在 / 添加失败”。
- 去掉表单原来的必选 checkbox，避免浏览器原生表单验证直接拦截提交。
- 修复队列按钮有焦点时不刷新，导致暂停后仍显示旧按钮的问题。

## 你现在怎么更新（重要）

**必须同时更新 `native` 和 `extension`，只换其中一个不能解决问题。**

1. 在 `chrome://extensions/` 暂时关闭 Hybrid 扩展开关；关闭已经打开的旧任务控制台。
2. 在旧的本机 Python 窗口按 `Ctrl+C` 退出。等退出后备份原 `data` 目录。不要同时启动两套引擎。
3. 解压 0.7.1 压缩包，把里面 `artwork-archive-hybrid` 文件夹中的文件复制到你原来运行的项目目录，覆盖同名源码和启动器。
   - **保留原 `data` 和 `.venv`**；新包没有这两个目录，不会要求你清空它们。
   - 尽量沿用原项目路径，以保留 Chrome 已登记的扩展 ID。
4. 双击更新后的 `START-WINDOWS.bat`，窗口标题信息应显示 **Hybrid 0.7.1**。
5. 在浏览器打开：

   `http://127.0.0.1:47653/v1/health`

   应看到类似：

   ```json
   {"service":"artwork-archive-hybrid","version":"0.7.1","protocol":2}
   ```

   - 无法访问：本机程序没有运行或回环连接被阻止，先看启动窗口错误。
   - 显示旧错误而不是上面的版本：可能还有旧 Python 进程占用端口，回到它的窗口正常退出。
6. 回到 `chrome://extensions/`，重新启用扩展并点击该扩展的重新加载按钮，确认版本为 **0.7.1**。无需卸载扩展。
7. 再点击扩展图标打开**新控制台**。如果已自动显示连接成功，旧配对记录已复用；否则粘贴当前本机窗口的令牌连接。
8. 看到绿色“本机引擎已连接”后直接添加作品或主页，无需再勾选许可。应出现任务成功提示和队列记录。

如果换了项目路径导致扩展 ID 改变，只需对新扩展重新配对，不需要删除 SQLite 数据。

## 已验证与未验证

本次实际运行了 Chromium 151 的已加载扩展，不再只用模拟 Chrome API：

- 复现旧版本 POST 配对成功、GET 状态读取 403 的现象。
- 新扩展连接旧服务时明确提示版本不兼容。
- 新版服务自检、错误令牌提示、正确令牌配对及随后 GET 状态读取通过。
- 无 checkbox 添加任务，成功提示及队列记录通过。
- 用本地模拟的米画师页面跑通主页发现、详情解析和直链记录入库；没有访问真实米画师页面。
- 暂停按钮刷新、关闭重开控制台后重连通过。
- 42 项 Python/API 测试、10 项调度模拟测试通过。

**仍未在 Windows 本机执行 BAT，亦未完成米画师登录实站验收。** 因此不把 Linux Chromium 的连接验证说成 Windows 全流程已验证。

如果仍卡住，请只提供配对按钮旁的新错误文字，以及 `/v1/health` 是否显示 0.7.1；不要发送配对令牌或 Cookie。
