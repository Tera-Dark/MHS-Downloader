# 安全说明与问题报告

## 报告方式

如果实际 GitHub 仓库已启用 Private vulnerability reporting，请优先使用 Security 页的私密报告入口。若尚未启用，请先通过仓库公开渠道请求私密联系方法，仅描述问题类别，不公开凭据、私人 URL、作品内容或可直接滥用的细节。

这里不虚构维护者邮箱，也不保证固定响应时间。仓库所有者应在发布前配置实际的私密报告渠道。

## 关注范围

- 数据或凭据意外外传；诊断中泄漏用户输入。
- 不受信任页面数据引发的脚本执行或危险文件名。
- ZIP 路径穿越、任务恢复错误导致的误报成功。
- 权限扩大、远程代码加载、测试配置进入发行包。
- 在明确停止或访问被拒绝后继续发起扩展任务。

官网自身正常发送的请求、站点对访问频率的限制，以及第三方图像本身的版权，不等于本扩展的安全漏洞，但可能需要改进提示或限制。

## 现有措施及非保证

扩展不读取 Cookie，无开发者接收服务器，诊断通过字段白名单构造；文件名经过限制，ZIP 只接受相对路径；响应和内存均有限额，测试与发行分离。

这些是风险控制，不是“绝对安全”证明。静态检查和模拟测试不等于完整安全审计。用户仍应按实际许可使用，并先小批量验证。

请不要为了验证扩展问题攻击真实站点、使用他人账号或绕过验证码。优先制作本地最小复现。
