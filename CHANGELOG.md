# 版本记录

## 0.6.0 — 2026-09-14

- 拆分组合根、事件控制、任务、导航、扫描、运行、呈现、下载与恢复服务；持久层、图片校验和休眠检测各自拥有生命周期。
- IndexedDB 按任务和元数据键增量写入，提交版本保护并发修改；旧 stateV2 一次性事务迁移，失败保留原始数据。
- 图片按至多 1 MB 块暂存；验证后与来源记录原子提交，工作台关闭后可恢复、离线保存。保存完成后或明确删除时清理暂存。
- 单 Worker 串行解码验证，头部尺寸预检、缩小验证、增量 SHA-256 / CRC32；Blob 引用式 ZIP 不再拼整卷字节数组。保留原图片字节，不降画质。
- ZIP 和原生下载先写请求日志，再调用下载 API；补齐 API 成功但 ID 尚未持久化的恢复窗口。不确定结果阻止自动重存，提供明确核对入口。
- 长间隔、冻结与时钟跳变触发保守停止，不在恢复后自动采集。
- 任务 URL 索引、队列分页渲染缓存和进度摘要去重；新增 5000 条真实 IndexedDB 测试、关闭恢复、事务中止、迁移失败与大图校验回归。
- 未增加权限、运行时依赖、站点范围或绕过机制。

## 0.5.0 — 2026-09-14

### 用户流程审查与可靠性加固

- 工作台生命周期独占锁、操作开始互斥、失效工作台登记探测，防止重复实例或双击并发写状态。
- 下载恢复核对来源，而非只信任下载 ID；导入记录不直接用于文件定位/取消。
- 导入保留有效图片候选，容忍无效条目并报告数量，检查合并上限，不信任空成功标记。
- 保存失败显示未保存警告，仍可导出内存备份；损坏的顶层结构不覆盖，提供原始恢复副本。
- 401/403/429 和已识别登录提示形成持久化访问暂停；429 尊重 Retry-After 且至少等待 60 秒，只能人工解除，不自动恢复请求。
- ZIP 图片拒绝重定向，退出时中止响应；原生拒绝/未授权错误也需人工解除暂停。
- 10 分钟内就绪候选可复用，减少重复详情页请求；重置任务清除候选。
- 新增独立 core-policy 纯规则模块及故障回归；工作台仍有单文件耦合，未宣称完成全面架构重写。
- 界面压缩宣传区、扩大队列、增强小字对比度、桌面状态栏置顶、空预览隐藏、明确队列下载计数与收集进度区别。

## 0.4.0 — 2026-09-14

- 重做浮动入口：右侧小图标、可拖动/记忆位置、收起/隐藏/重置、逐次许可、进度与停止、窄视口适配。
- 支持从官网后台启动收集或 ZIP 下载；没有工作台时创建非活动标签页，启动时不激活原窗口或任务页。
- 先分段滚动批量收集 DOM 作品链接，再用正常卡片导航解析缺少的 URL，保留原页面地址与滚动位置。
- 增加只解析图片直链的后台路径、队列批量解析及去重 TXT 导出；不把缩略图改写为原稿地址。
- 后台部分结果保留待确认，不弹模态确认框、不会自动下载部分结果。
- 工作台使用会话标签页 ID 登记，不依赖广泛 tabs 权限查询扩展页地址；修复由此导致的停止入口找不到工作台问题。
- 权限未增加；保留 0.3.1 导航修复、401/403/429 与登录停止、ZIP/原生下载和记录恢复。

## 0.3.1 — 2026-09-14

### 修复：主页扫描在首件作品后中断

- 识别到作品 URL 后先持久化到队列及扫描记录，再尝试返回主页；返回失败也保留该作品。
- 浏览器缺少后退历史，或后退没有返回原主页时，只对已知公开作品/主页/空白页执行一次原主页地址恢复，不依赖历史记录一定存在。
- 恢复列表时有界重放滚动；点击及返回遵守页面间隔。无法恢复则停止，不循环刷新。
- 明确的 HTTP 401 / 403 / 429、登录/验证弹窗和未知重定向仍停止，不作为导航恢复理由。
- 普通导航错位不再混写为“要求登录”；空下拉选择不再被 `Number('')` 转为标签页 0，已关闭标签页给出重新选择提示。
- 增加浮动按钮入口、SPA `replaceState`、`location.replace`、缺少历史、链接保留及登录停止的浏览器回归；保留之前的测试。

本次根据用户截图定位，并在旧 0.3.0 安装包注入同款历史错误复现“已打开作品但保留 0 件”。模拟复现不是对实站内部路由实现的确认，详见验证记录。

## 0.3.0 — 2026-09-14

### 用户体验
- 默认新手视图与可收起的首次使用引导；高级参数不再一开始全部展开。
- 统一输入识别主页、作品链接及常见分享文字，支持 Ctrl / ⌘ + Enter。
- 增加推荐、慢速轻量、较快三档预设，不修改图片质量或作品范围。
- 搜索、状态筛选、分页、勾选下载、逐项重置及移除记录。
- 快速流程只处理本次输入 / 主页发现的范围，避免顺带处理旧队列。
- 通俗错误提示、任务网页入口、下载文件夹和最近文件定位。
- 独立诊断对话框及 JSON 导出，不包含私人来源或完整日志。
- 简化进度区、运行时间、键盘焦点及窄屏布局。

### 工程和设计
- 重绘图标，保留 SVG 及多尺寸 PNG；增加深浅背景图标预览。
- 新建干净的 extension / docs / design / tests / tools 仓库结构。
- 可重复、根目录平铺的扩展打包，附 SHA-256 校验。
- CI、单元测试、本地模拟浏览器回归、Issue / PR 模板。
- ZIP 文件名与重复条目检查，避免相对路径穿越。
- 初始化期间禁止空状态覆盖已有记录；保留旧任务格式兼容。

## 0.2.0 — 2026-09-14

- 主页自动收集：链接、常规卡片导航、滚动加载与部分结果报告。
- 页面读取与下载分离，有限并发、分卷 ZIP、内容去重、多图候选。
- 停止后可保存当前缓冲，旧版任务 JSON 导入。
- 缩减主机范围，移除广泛 tabs 权限。

## 0.1.1 — 2026-09-14

- 修复发行 ZIP 多套一层目录导致选错加载目录的问题。
- 从打包后的解压目录验证扩展加载和模拟下载流程。

## 0.1.0 — 2026-09-14

- 首个详情页队列、预览、串行下载、URL 去重和来源清单原型。
